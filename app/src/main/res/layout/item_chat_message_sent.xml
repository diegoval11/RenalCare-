<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_chat_message_sent.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="end"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:maxWidth="280dp">

        <!-- Imagen (si existe) -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cv_image_container_sent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="2dp">

            <ImageView
                android:id="@+id/iv_message_image_sent"
                android:layout_width="200dp"
                android:layout_height="200dp"
                android:scaleType="centerCrop"
                android:visibility="gone"
                android:contentDescription="Imagen enviada"
                tools:visibility="visible"
                tools:src="@drawable/ic_placeholder" />

        </androidx.cardview.widget.CardView>

        <!-- Texto del mensaje -->
        <TextView
            android:id="@+id/tv_message_sent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_message_sent"
            android:padding="12dp"
            android:textColor="@color/white"
            android:textSize="16sp"
            tools:text="Mensaje enviado" />

    </LinearLayout>

</LinearLayout>